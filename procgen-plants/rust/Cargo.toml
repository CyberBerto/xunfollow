[package]
name = "procgen-plants"
version = "0.1.0"
edition = "2021"
description = "Rule-driven L-system plants growing across a procedural landscape"

[dependencies]
macroquad = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
